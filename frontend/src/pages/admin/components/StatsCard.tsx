import { Card } from "@/components/ui/card"

type StatsCardProps={
    icon:React.ElementType;
    label:string;
    value:string;
    bgColor:string;
    iconColor:string;


}

const StatsCard= ({bgColor,icon,iconColor,label,value}:StatsCardProps) => {
  return (
    <Card>

    </Card>
  )
}

export default StatsCard